"use client";

/**
 * Placeholder: the FEEDS package replaces this with the real 7-day USGS flow sparkline.
 * Props are the agreed contract; rendering nothing keeps the Conditions card layout stable.
 */
export interface FlowSparklineProps {
  parkId: string;
  siteId: string | null;
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function FlowSparkline(props: FlowSparklineProps): null {
  return null;
}
